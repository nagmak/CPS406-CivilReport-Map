extends layout

block link
  -var selected = 'Home';

block content
    .container
        if !user
            p You are currently logged in as a Guest.
        if user
            p You are currently logged in as #{user.username}
            a.button.btn.btn-default(type='button' href='/logout') Logout

    h1= title
    p Welcome to the #{title}
        .row
            .col-md-12
                .page-header
                    h1 Please right click the street where the problem is located
    div#map
    include modal.pug
//-    .row
        .col-md-12    
            div#map

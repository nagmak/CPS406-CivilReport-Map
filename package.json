{
  "name": "civil-report-map",
  "version": "1.0.0",
  "description": "Loads a Civil Report Map for the City of toronto",
  "scripts": {
    "start": "harp server --port $PORT"
  },
  "dependencies": {
    "harp": "*"
  }
}